---
paths: "**/*.py, **/python/**, **/requirements.txt, **/pyproject.toml"
---

# Python高速化ルール

> 参考: サプーチャンネル「Pythonを速くさせる方法13個」+ 2025-2026年ベストプラクティス

## 🔴 高速化の基本原則

1. **まずプロファイリング** → ボトルネック特定が最優先
2. **適切なツール選択** → 処理の性質に応じて選ぶ
3. **Python 3.13+を使用** → JITコンパイラ・GIL改善

## プロファイリングツール（必須）

| ツール | 用途 | コマンド |
|--------|------|----------|
| **Scalene** | CPU/メモリ/GPU詳細分析 | `scalene script.py` |
| **py-spy** | 本番環境デバッグ | `py-spy top --pid PID` |
| **cProfile** | 標準プロファイラ | `python -m cProfile script.py` |

## 高速化テクニック一覧

### Tier 1: 手軽な改善

| テクニック | 説明 | 例 |
|------------|------|-----|
| **内包表記** | forループより高速 | `[x*2 for x in lst]` |
| **適切なデータ構造** | set/deque活用 | 存在確認はset、両端操作はdeque |
| **functools.lru_cache** | 関数キャッシング | `@lru_cache(maxsize=128)` |
| **ジェネレータ** | メモリ効率化 | `(x for x in range(1000000))` |

### Tier 2: ライブラリ活用

| ライブラリ | 用途 | 高速化倍率 |
|------------|------|-----------|
| **NumPy** | 数値計算・ベクトル演算 | 10-100x |
| **Polars** | データフレーム処理 | Pandasの5-10x |
| **Numba** | JITコンパイル | 100x以上 |
| **orjson** | JSON処理 | 標準の10x |

### Tier 3: コンパイル・言語連携

| ツール | 用途 | 難易度 |
|--------|------|--------|
| **Cython** | PythonをCにコンパイル | 中 |
| **PyO3/maturin** | Rust連携 | 高 |
| **f2py** | Fortran連携 | 高 |
| **PyPy** | Python代替実装 | 低（ただし互換性注意） |

### Tier 4: 並行・並列処理

| 処理タイプ | 推奨 | 例 |
|------------|------|-----|
| **CPUバウンド** | multiprocessing | 計算処理 |
| **I/Oバウンド** | asyncio/threading | API呼び出し、ファイルI/O |
| **混合** | concurrent.futures | ThreadPool/ProcessPool |

## Python 3.13+ 新機能

| 機能 | 説明 |
|------|------|
| **JITコンパイラ** | 自動で高速化（実験的） |
| **Per-interpreter GIL** | マルチスレッド効率向上 |
| **GIL無効化オプション** | `--disable-gil` |

## Rust製高速ツール（推奨）

| ツール | 用途 | 従来比 |
|--------|------|--------|
| **uv** | パッケージ管理 | pip比10-100x |
| **Ruff** | リンター/フォーマッター | 100x |
| **Polars** | データ処理 | Pandas比5-10x |

## コード例

### Numba JIT
```python
from numba import jit

@jit(nopython=True)
def fast_sum(arr):
    total = 0
    for x in arr:
        total += x
    return total
```

### Polars（Pandas代替）
```python
import polars as pl

df = pl.read_csv("data.csv")
result = df.filter(pl.col("value") > 100).group_by("category").agg(pl.sum("value"))
```

### asyncio非同期処理
```python
import asyncio
import aiohttp

async def fetch_all(urls):
    async with aiohttp.ClientSession() as session:
        tasks = [session.get(url) for url in urls]
        return await asyncio.gather(*tasks)
```

## 禁止事項

| 禁止 | 代替 |
|------|------|
| 素のforループで数値計算 | NumPy/Numba使用 |
| 大量データにPandas | Polars使用 |
| 同期処理でAPI大量呼び出し | asyncio使用 |
| pipでパッケージ管理 | uv使用 |

## 参照

- [Scalene](https://github.com/plasma-umass/scalene) - 高機能プロファイラ
- [Polars](https://pola.rs/) - 高速データフレーム
- [Numba](https://numba.pydata.org/) - JITコンパイラ
- [uv](https://github.com/astral-sh/uv) - 高速パッケージ管理
